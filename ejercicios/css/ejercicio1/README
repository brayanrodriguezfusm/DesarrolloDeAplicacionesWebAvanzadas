A partir del código HTML y CSS, añadir los selectores CSS que faltan para aplicar
los estilos. Cada regla CSS incluye un comentario en el que se explica los elementos a
los que debe aplicarse los selectores